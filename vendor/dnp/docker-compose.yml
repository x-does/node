services:
  pong:
    build: .
    container_name: definitely-not-pong
    restart: unless-stopped
    ports:
      - "8080:8080"
    environment:
      NODE_ENV: production
      HOST: 0.0.0.0
      PORT: 8080
      # ALLOWED_ORIGINS: https://your-domain.example
